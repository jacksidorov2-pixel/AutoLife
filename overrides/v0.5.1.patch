--- a/app/src/main/java/com/autolife/MainActivity.java
+++ b/app/src/main/java/com/autolife/MainActivity.java
@@ -136,13 +136,14 @@
         getWindow().addFlags(WindowManager.LayoutParams.FLAG_KEEP_SCREEN_ON);
         getWindow().setStatusBarColor(Color.BLACK);
         getWindow().setNavigationBarColor(Color.BLACK);
-        enterImmersive();
+        // Apply immersive mode only after the decor/content is attached.
 
         usbManager = (UsbManager) getSystemService(USB_SERVICE);
         transport = ((AutoLifeApp) getApplication()).getTransport();
         transport.addListener(transportListener);
 
         buildCarUi();
+        getWindow().getDecorView().post(this::enterImmersive);
         registerUsbReceiver();
         refreshTrace();
         refreshStateChips();
@@ -570,24 +571,38 @@
     }
 
     private void enterImmersive() {
+        final View decor = getWindow().getDecorView();
+        if (decor == null) return;
+
         if (Build.VERSION.SDK_INT >= 30) {
-            WindowInsetsController controller = getWindow().getInsetsController();
+            // On HyperOS/Android 15 the Activity can reach onCreate before the decor
+            // has an InsetsController. Calling Window#getInsetsController() there may
+            // throw inside PhoneWindow, so query the decor view and gracefully fall back.
+            WindowInsetsController controller = decor.getWindowInsetsController();
             if (controller != null) {
                 controller.hide(WindowInsets.Type.statusBars() | WindowInsets.Type.navigationBars());
                 controller.setSystemBarsBehavior(WindowInsetsController.BEHAVIOR_SHOW_TRANSIENT_BARS_BY_SWIPE);
+            } else {
+                applyLegacyImmersive(decor);
+                decor.postDelayed(this::enterImmersive, 100L);
             }
         } else {
-            getWindow().getDecorView().setSystemUiVisibility(
-                    View.SYSTEM_UI_FLAG_IMMERSIVE_STICKY |
-                            View.SYSTEM_UI_FLAG_FULLSCREEN |
-                            View.SYSTEM_UI_FLAG_HIDE_NAVIGATION |
-                            View.SYSTEM_UI_FLAG_LAYOUT_FULLSCREEN |
-                            View.SYSTEM_UI_FLAG_LAYOUT_HIDE_NAVIGATION |
-                            View.SYSTEM_UI_FLAG_LAYOUT_STABLE
-            );
+            applyLegacyImmersive(decor);
         }
     }
 
+    @SuppressWarnings("deprecation")
+    private void applyLegacyImmersive(View decor) {
+        decor.setSystemUiVisibility(
+                View.SYSTEM_UI_FLAG_IMMERSIVE_STICKY |
+                        View.SYSTEM_UI_FLAG_FULLSCREEN |
+                        View.SYSTEM_UI_FLAG_HIDE_NAVIGATION |
+                        View.SYSTEM_UI_FLAG_LAYOUT_FULLSCREEN |
+                        View.SYSTEM_UI_FLAG_LAYOUT_HIDE_NAVIGATION |
+                        View.SYSTEM_UI_FLAG_LAYOUT_STABLE
+        );
+    }
+
     private int dp(int value) {
         return Math.round(value * getResources().getDisplayMetrics().density);
     }
--- a/app/build.gradle
+++ b/app/build.gradle
@@ -10,8 +10,8 @@
         applicationId 'com.autolife'
         minSdk 29
         targetSdk 36
-        versionCode 5
-        versionName '0.5.0'
+        versionCode 6
+        versionName '0.5.1'
     }
 
 
